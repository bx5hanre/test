rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Only authenticated admin can upload/delete
    match /clinic-media/{file} {
      allow read:  if true;
      allow write: if request.auth != null;
    }
  }
}
